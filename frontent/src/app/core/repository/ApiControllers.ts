export enum ApiControllers {
  AUTH='auth',
  USERS = 'users',
  PATIENTS = 'patients',
  SEARCH = 'search',
  SAVED_SEARCH = 'savedSearches',
  SEARCH_CONFIGURATION='searchConfiguration',
  FILES='files',
}
