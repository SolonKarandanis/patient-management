import { DisallowedRolesDirective } from './disallowed-roles.directive';

describe('DisallowedRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new DisallowedRolesDirective();
    expect(directive).toBeTruthy();
  });
});
