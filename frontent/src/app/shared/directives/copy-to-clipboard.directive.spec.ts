import { CopyToClipboardDirective } from './copy-to-clipboard.directive';

xdescribe('CopyToClipboardDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyToClipboardDirective();
    expect(directive).toBeTruthy();
  });
});
